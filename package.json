{
  "name": "float-bubble-room",
  "version": "1.0.0",
  "description": "Single-room floating transfer bubble with live sync.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test",
    "init-config": "node scripts/init-config.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "qrcode": "^1.5.4",
    "ws": "^8.18.3"
  }
}
